import ReactDOM from 'react-dom';
import React from 'react';
import CostContainer from './components/CostContainer.js';

ReactDOM.render(
  <CostContainer />, 
  document.getElementById('mount-point')
);